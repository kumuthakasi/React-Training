import React from 'react';
import { BrowserRouter, Route, Switch, Link } from 'react-router-dom';

class Dashboard extends React.Component {
    render() {
        return (
            // <Link to="/prefer">Application</Link>
            <div>Dashboard</div>
        )
    }
}
export default Dashboard